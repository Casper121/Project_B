import React from 'react';
import { Icon, IconProps } from '@ui-kitten/components';

const CameraIcon = (props?: Partial<IconProps>): React.ReactElement => (
  <Icon {...props} name="camera-outline" />
);

export default CameraIcon;